import { MuseumHeader } from "crossroad-collection";

// The museum "facade": department line, the ARCHIVE title in the Ultra display
// face, subtitle, and the plaque of facade counts. No props — it reads the
// catalog baked into the bundle.
export function Facade() {
  return <MuseumHeader />;
}
