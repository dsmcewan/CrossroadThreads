import { MuseumFooter } from "crossroad-collection";

// The closing colophon — italic body face, muted brown. "The gift shop is the
// museum."
export function Default() {
  return <MuseumFooter />;
}
